openapi: 3.0.0
info:
  title: Sample API
  description: A simple API to demonstrate OpenAPI specs.
  version: "1.0.0"
  contact:
    name: API Support
  license:
    name: MIT
paths:
  /example:
    get:
      summary: Example endpoint
      responses:
        '200':
          description: Success
components:
  schemas:
    ExampleSchema:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string