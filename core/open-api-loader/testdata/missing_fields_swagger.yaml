swagger: "2.0"
info:
  title: Minimal API
  version: "1.0.0"
paths:
  /ping:
    get:
      summary: Ping the server
      responses:
        200:
          description: pong
definitions:
  PingResponse:
    type: object
    properties:
      message:
        type: string